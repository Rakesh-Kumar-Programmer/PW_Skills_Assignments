//Problem 3

// Nested ternary oprator

const number = 1; // to assign 1 value to number variable.

let result = number > 0 ? 'Postive' : number < 0 ? 'Negative' : 'Zero'; // Nested ternary oprator

console.log(`This number ${number} is :${result}`); // result is postive when vale is 1