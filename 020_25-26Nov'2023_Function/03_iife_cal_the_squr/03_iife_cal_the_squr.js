// IIFE function
(function() {
    // Define a variable with the number who's square requaire
    var number = 5;

    // Calculate the square
    var square = number * number;

    // Display the result
    console.log("The square of", number, "is", square);
})();
